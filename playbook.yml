---
- hosts: localhost
  become: true
  roles:
    - { role: base, tags: ['base'] }
    - { role: nettools, tags: ['nettools'] }
  vars_prompt:
    - name: user_password
      prompt: "Enter desired user password"
